<div class="floating">
  <dt>
    {{#if isEditing}}
      {{view DashIt.EditProjectView}}
    {{else}}
      <p>{{project.name}}</p>
    {{/if}}
    <div class="commands">
      {{#unless isEditing}}
        <a href="#" {{action "showEdit"}}>edit</a>
        <a href="#" {{action "destroyRecord"}}>delete</a>
      {{/unless}}
    </div>
  </dt>
</div>
