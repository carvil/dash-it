<dl>
  <dt>
  {{#each projects}}
    {{view DashIt.ShowProjectView projectBinding="this"}}
  {{/each}}
  {{#if isNewVisible}}
    <dt>
      {{view DashIt.NewProjectView}}
    </dt>
  {{/if}}
  </dt>
</dl>

<hr>
<div class="commands">
  <a href="#" {{action "showNew"}}>New Project</a>
  <a href="#" {{action "refreshListing"}}>Refresh Listing</a>
</div>
